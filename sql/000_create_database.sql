-- Database creation. Normally done from the shell:
--     createdb llm_question_log
-- This file is the SQL alternative, runnable as the postgres superuser
-- when `createdb` is not on PATH (some Windows installations):
--     psql -U postgres -f sql/000_create_database.sql
CREATE DATABASE llm_question_log;
